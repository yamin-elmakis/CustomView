<?xml version="1.0" encoding="utf-8"?>
<merge
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:parentTag="RelativeLayout">

    <LinearLayout xmlns:app="http://schemas.android.com/apk/res-auto"
        android:id="@+id/sp_hint_container"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:background="@color/purple_faded"
        android:orientation="vertical"
        android:padding="5dp">

        <com.example.customview.TriangleView
            android:id="@+id/sp_hint_arrow_up"
            android:layout_width="20dp"
            android:layout_height="15dp"
            android:layout_marginBottom="-1dp"
            android:rotation="180"
            app:triangle_color="@color/color_back_2" />

        <FrameLayout
            android:id="@+id/sp_hint_inner_container"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:orientation="horizontal" />

        <com.example.customview.TriangleView
            android:id="@+id/sp_hint_arrow_down"
            android:layout_width="20dp"
            android:layout_height="15dp"
            app:triangle_color="@color/color_back_2" />
    </LinearLayout>

</merge>